# SAGE File Format: CDX

There are two different CDX file types referenced in the game EXE.

- `spheres.cdx`
- `collide.cdx`

Only `spheres.cdx` is ever used, and `collide.cdx` does not appear fully developed.
